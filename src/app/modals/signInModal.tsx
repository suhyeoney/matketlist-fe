'use client'

interface SignInModalProps {
  htmlString: string,
};

const SignInModal: React.FC<SignInModalProps> = ({ htmlString }) => {
  return (
    <>
    </>
  );
};

export default SignInModal;
